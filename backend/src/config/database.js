// backend/src/config/database.js

const config = {
    host: 'localhost',      // Seu endereço do MySQL
    user: 'root',           // Seu usuário do MySQL
    password: 'sua_senha_secreta', // Sua senha do MySQL
    database: 'fittrack_db',// Nome do seu banco de dados
    port: 3306
};

module.exports = config;